<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Lebonplan{% endblock %}</title>
    <link rel="stylesheet" href="/assets/style.css">
</head>
<body>

    <!-- Logo et titre -->
    <div class="title center">
        <img src="/assets/logo-lbp-header.png" alt="Lebonplan - Le meilleur site d'annonces en ligne">
    </div>

    <!-- Navigation pour écrans larges -->
    <nav class="navbar">
        <div class="bar">
            <a href="{{ path('home') }}" class="{% if current_page == 'home' %}selected{% endif %}">Accueil</a>
            <a href="{{ path('entreprises') }}" class="{% if current_page == 'entreprises' %}selected{% endif %}">Entreprises</a>
            <a href="{{ path('offres') }}" class="{% if current_page == 'offres' %}selected{% endif %}">Offres</a>
            <a href="{{ path('whishlist') }}" class="{% if current_page == 'whishlist' %}selected{% endif %}">Whishlist</a>
            <a href="{{ path('contact') }}" class="{% if current_page == 'contact' %}selected{% endif %}">Contact</a>
        </div>
        <div class="account">
            <a href="{{ path('connexion') }}">Connexion</a>
            <a href="{{ path('inscription') }}" class="inscription">S'inscrire</a>
        </div>
    </nav>

    <!-- Menu burger pour mobile -->
    <div class="burger">
        <div class="burger-icon">
            <img src="/assets/burger-icon.png" alt="Menu">
        </div>
        <div class="bar">
            <a href="{{ path('home') }}">Accueil</a>
            <a href="{{ path('entreprises') }}">Entreprises</a>
            <a href="{{ path('offres') }}">Offres</a>
            <a href="{{ path('whishlist') }}">Whishlist</a>
            <a href="{{ path('contact') }}">Contact</a>
        </div>
    </div>

    <!-- Contenu principal -->
    <main>
        {% block content %}{% endblock %}
    </main>

    <!-- Pied de page -->
    <footer class="footer center">
        &copy; {{ "now"|date("Y") }} Lebonplan - Tous droits réservés
    </footer>

</body>
</html>
