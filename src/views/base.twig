<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Lebonplan{% endblock %}</title>
    <link rel="stylesheet" href="/assets/style.css">
    <link rel="shortcut icon" href="/assets/images/logo_icon.png" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <div class="title center">
        <img src="/assets/images/logo_full.png" alt="Lebonplan - Le meilleur site d'annonces en ligne">
    </div>

    <nav class="navbar">
        <div class="bar">
            <a href="{{ path('home') }}">Accueil</a> <span>|</span>
            <a href="{{ path('entreprises') }}">Entreprises</a> <span>|</span>
            <a href="{{ path('offres') }}">Offres</a> <span>|</span>
            <a href="{{ path('whishlist') }}">Wishlist</a> <span>|</span>
            <a href="{{ path('contact') }}">Contact</a>
        </div>
        <div class="account">
            {% if user %}
                <div class="user-dropdown">
                    <span class="username"><i class="fas fa-user-circle"></i> {{ user.name }}</span>
                    <div class="dropdown-content">
                        <a href="{{ path('profile') }}">Mon profil</a>
                        <a href="{{ path('logout') }}">Déconnexion</a>
                    </div>
                </div>
            {% else %}
                <a href="{{ path('login') }}" class="connexion-inscription-btn">
                    <i class="fas fa-right-to-bracket"></i> Connexion
                </a>
            {% endif %}
        </div>
    </nav>

    <main>
        <section class="accueil-intro center">
            <h1>Bienvenue sur Lebonplan !</h1>
            <p>Vous cherchez un stage ou un emploi ? Découvrez nos offres exclusives et les meilleures entreprises !</p>
            <a href="{{ path('offres') }}" class="btn-primary">Voir les offres</a>
        </section>

        <section class="actualites">
            <h2>📢 Actualités & Offres à la une</h2>
            <div class="offre">
                <p><strong>Nouvelle offre chez IBM :</strong> Développeur Python - Stage 6 mois</p>
                <a href="{{ path('offres', {'id': 1}) }}" class="btn-secondary">Postuler</a>
            </div>
            <div class="offre">
                <p><strong>Capgemini recrute :</strong> Ingénieur logiciel C++</p>
                <a href="{{ path('offres', {'id': 2}) }}" class="btn-secondary">Voir l'offre</a>
            </div>
        </section>

        <section class="raccourcis">
            <h2>🚀 Accès rapide</h2>
            <div class="actions">
                <a href="{{ path('offres') }}" class="btn-shortcut">🔎 Trouver un stage</a>
                <a href="{{ path('entreprises') }}" class="btn-shortcut">🏢 Voir les entreprises</a>
                <a href="{{ path('contact') }}" class="btn-shortcut">📩 Nous contacter</a>
            </div>
        </section>

        <section class="aide">
            <h2>Besoin d'aide ?</h2>
            <p>Découvrez comment optimiser votre profil et postuler efficacement.</p>
            <a href="{{ path('help') }}" class="btn-secondary">Guide d'utilisation</a>
        </section>
    </main>

    <footer class="footer center">
        &copy; {{ "now"|date("Y") }} Lebonplan - Tous droits réservés - <a href="{{ path('legal-notice') }}">Mention Légale</a>
    </footer>

    <script src="/assets/code.js"></script>
</body>
</html>
